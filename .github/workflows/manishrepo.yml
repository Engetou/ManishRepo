name: learn github actions

on:
    push:
        branches:
            - main

    workflow_dispatch:

jobs: 

    myfirstjob:
        runs-on: ubuntu-latest
        steps: 
            - name: clone my repository
              uses: actions/checkout@v4
    
  

