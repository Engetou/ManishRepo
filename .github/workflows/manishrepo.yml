name: learn github actions

on:
    push:
        branches:
            - main

    workflow_dispatch:

jobs: 

    myfirstjob:
        runs-on: ubuntu-latest
        steps: 
            - name: clone my repository
              run: echo "i'am learning DevOps"

